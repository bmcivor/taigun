# E1 — Infrastructure & connectivity

Gate epic. Ensures Taiga's Postgres instance is reachable from the dev machine
before any tool code is written.

## Tickets

- 001 — Expose Postgres over Tailscale in vertex-studio
- 002 — Verify DB connectivity from dev machine

## Blocks

- E2 (can scaffold in parallel, but DB work in E4 needs 002 confirmed)
