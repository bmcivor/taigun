services:
  test:
    build:
      context: .
      target: test
    command: uv run pytest tests/ -v
    volumes:
      - .:/app

  release:
    build:
      context: .
      target: release
    volumes:
      - .:/app
